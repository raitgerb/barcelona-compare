---
// SUPERSEDED — district pages moved to ../[x]/index.astro which resolves
// both district and city-service slugs. This stub intentionally generates
// zero paths to avoid URL collisions. Safe to delete in any future cleanup.
export async function getStaticPaths() {
  return [];
}
---
